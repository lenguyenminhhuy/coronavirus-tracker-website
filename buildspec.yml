version: 0.2

phases:
  pre_build:
    commands:
      - cd frontend
      - npm install
  build:
    commands:
      - cd frontend
      - npm run build

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: build